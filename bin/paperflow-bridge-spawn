#!/usr/bin/env bash
# Backwards-compat shim. paperflow-bridge-spawn was the SessionStart hook
# that spawned the per-instance claude-bridge daemon. The new model
# registers the session with the consolidated paperflow-daemon instead.
exec "$(dirname "$0")/paperflow-session-register" "$@"
